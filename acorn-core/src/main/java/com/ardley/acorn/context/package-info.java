/**
 * Framework-agnostic request context abstraction.
 *
 * <p>Provides {@link com.ardley.acorn.context.RequestContext} — a portable
 * representation of HTTP request metadata that extractors use to resolve
 * identifiers without coupling to a specific HTTP framework.
 */
package com.ardley.acorn.context;
