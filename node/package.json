{
  "name": "acorn-node",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@ardley-technologies/acorn-core' build && bun run --filter '!@ardley-technologies/acorn-core' build",
    "test": "bun test",
    "clean": "rm -rf packages/*/dist"
  }
}
