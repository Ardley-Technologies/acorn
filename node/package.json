{
  "name": "acorn-node",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@ardley/acorn-core' build && bun run --filter '!@ardley/acorn-core' build",
    "test": "bun test",
    "clean": "rm -rf packages/*/dist"
  }
}
