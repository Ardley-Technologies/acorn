/**
 * Permission data model — sets, levels, scope filters, and attribute filters.
 *
 * <p>Defines the structure of role permission configurations and the scope filtering
 * rules used to restrict access to resources matching specific attribute conditions.
 */
package com.ardley.acorn.permission;
